<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converter="clr-namespace:PracticalShooterApp.Converters;assembly=PracticalShooterApp"
             x:Class="PracticalShooterApp.Controls.WebImageView">
    <ContentView.Resources>
        <ResourceDictionary>
            <converter:UrlToImageSourceConverter x:Key="UrlToImageSourceConverter"/>
        </ResourceDictionary>
    </ContentView.Resources>
    <ContentView.Content>
        <StackLayout>
            <Frame CornerRadius="15" 
                   Padding="0"
                   Margin="5"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   IsClippedToBounds="True">
                <Image x:Name="WebImage"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       Source="{Binding UrlSource, Converter={StaticResource UrlToImageSourceConverter}}">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer
                            Tapped="OnWebImageTapped"
                            NumberOfTapsRequired="1" />
                    </Image.GestureRecognizers>
                </Image>
            </Frame>
        </StackLayout>
    </ContentView.Content>
</ContentView>